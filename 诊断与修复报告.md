# 二游辅助「安装即坏」诊断与修复报告

> 日期：2026-09-20 ｜ 对象：`eryou-assistant-v0.2.0-win64-portable.zip`
> 源包 sha256：`a9262758eed31d6793c68544cee4cc1179382f3ca03d42ce8bd85ab7bb08db44`（214 MB，8153 个条目 / 7233 个文件）
> 结论：三个问题全部定位并修复，其中「粘贴链接不跳转视频」是真 Bug（不是配置问题），已实测恢复播放。

---

## 1. 解压出来一堆乱码

**根因**：zip 里的条目名是 UTF-8 字节，但**没有设置 zip 的 UTF-8 标志位**（general purpose bit 11）。
Windows 资源管理器 / `Expand-Archive` 遇到没标志位的条目名时按系统 ANSI 代码页解码，本机是 CP936（GBK），
于是 `二游辅助` 的 UTF-8 字节 `E4 BA 8C E6 B8 B8 E5 8A A9 E5 8A A9` 被当成 6 个 GBK 字 → `浜屾父杈呭姪`。

实测证据（直接读 zip 中央目录的原始字节）：

```
总条目        : 8153
UTF-8 标志位  : 0          ← 一个都没设
basename 含中文: 5 个
```

| 资源管理器解出来的（乱码） | 真实名字 |
|---|---|
| `浜屾父杈呭姪.exe` | `二游辅助.exe` |
| `鍘熺�瀇瀵硅瘽灏忎笁瑙�.png` | `原神_对话小三角.png` |
| `鍘熺�瀇瀵硅瘽閫夋嫨.png` | `原神_对话选择.png` |
| `鏄熺┕閾侀亾_瀵硅瘽灏忎笁瑙�.png` | `星穹铁道_对话小三角.png` |
| `鏄熺┕閾侀亾_瀵硅瘽閫夋嫨.png` | `星穹铁道_对话选择.png` |

注意 `�`：`原神` 那两条在 CP936 解码时落在字符边界上，**名字被不可逆地损坏**（出现替换字符），
所以「手动改名」是修不回来的——必须重新解压。

**修法**：`03_二游辅助\tools\extract_portable.py`。
它不用 zipfile 的默认解码，而是把条目名按 cp437 逆回原始字节再按 UTF-8 解码（`flag_bits & 0x800` 为 0 时才转），
写文件时顺带清除 `Zone.Identifier`（见第 3 节）。实测 8153 个条目全部还原，`二游辅助.exe` 与 4 张模板图名字正确：

```
03_二游辅助\app\二游辅助.exe
03_二游辅助\app\_internal\datas\imgs\template\原神_对话选择.png
03_二游辅助\app\_internal\datas\imgs\template\星穹铁道_对话选择.png
```

---

## 2. 东西在下载目录里

**处理**：

1. 便携版搬到 `C:\Users\16408\Desktop\二游自动\03_二游辅助\app\`（7233 个文件 / 528 MB）。
2. 原始 zip 从 `Downloads` 移到 `03_二游辅助\发布存档\` 留档（**没有删**：这份包目前无法重新构建，
   见第 4 节的说明；如果你确认不需要，直接删这个 214 MB 的 zip 即可）。
3. 下载目录里的乱码解压件已删除：`C:\Users\16408\Downloads\eryou-assistant-v0.2.0-win64-portable\` 已不存在。

---

## 3. 链接复制进去，不跳转到视频（真 Bug）

### 3.1 现象链

程序自己的日志（`%LOCALAPPDATA%\MabaoLocal\logs\2026-09-20.log`，你 12:22 那次运行）第 12 行就是崩溃：

```
09-20 12:22:06.471 [CRITICAL] [mabao.CRASH] 程序异常退出 (WebviewInitException):
    Critical Error: Failed to initialize .NET environment for WebView2.
  RuntimeError: Failed to resolve Python.Runtime.Loader.Initialize
      from ...\_internal\pythonnet\runtime\Python.Runtime.dll
```

也就是说：**内嵌浏览器（WebView2）根本没起来**。地址栏回车以后没有任何反应，不是「跳转失败」，
是那个浏览器控件本身是死的。同一份日志里 AI 导航（ONNX）是正常的（`backend=xfeat+angle detector=True error=none`），
只有 .NET/WebView2 这条链断了。

### 3.2 根因：Windows 的「来自 Internet」标记（Mark-of-the-Web）

从网上下载的 zip，用资源管理器「全部解压缩」时，**每个解出来的文件都会带一个 `Zone.Identifier`
备用数据流**（安全软件/浏览器看到的就是这个）。实测这份解压件：

```
带 Zone.Identifier 的文件 : 7230 / 7231
  _internal\pythonnet\runtime\Python.Runtime.dll      ← 就是它
  _internal\clr_loader\ffi\dlls\amd64\ClrLoader.dll
```

而 pythonnet 在 Windows 上默认用 .NET Framework（clr_loader 的 `netfx` 运行时）：
它通过 `ClrLoader.dll` 的 `pyclr_get_function(域, 程序集路径, "Python.Runtime.Loader", "Initialize")`
去反射加载 `Python.Runtime.dll`。**被标记为「来自 Internet」的程序集在 .NET Framework 里会被拒绝加载**，
于是返回空指针，pythonnet 报的就是上面那句 `Failed to resolve ...`。

### 3.3 证明（两处独立实验）

**(a) 文件本身没错，是标记的锅。** 先把便携版里的 DLL 与官方 wheel 对比：

```
便携版  _internal\pythonnet\runtime\Python.Runtime.dll
sha256  D204AD74DC18CD07320C8E665BD32EC6549B555CE97E61E4D3CF88437A64988E   450048 字节
pythonnet 3.0.5 官方 wheel 里的同名文件：
sha256  D204AD74DC18CD07320C8E665BD32EC6549B555CE97E61E4D3CF88437A64988E   （完全一致）
```

**(b) 只加一个标记就能复现，删掉就能恢复。** 在干净虚拟环境（Python 3.12.14 + pythonnet 3.0.5 + clr_loader 0.2.10）
里做 A/B：

```
A 不加标记：python -c "import clr, System; print(System.Environment.Version)"
            → OK 4.0.30319.42000
B 给 Python.Runtime.dll 加上 [ZoneTransfer] ZoneId=3：
            → 与程序日志完全同一条调用链失败
               File "...\clr_loader\types.py", line 97, in get_function
               File "...\clr_loader\netfx.py", line 49, in _get_callable
C 删掉标记：恢复 OK
```

顺带一个佐证：`MabaoLocal\logs\2026-08-18.log` 里是**同一个崩溃**（更早的一次构建），
说明这份便携版从下载解压那一刻起浏览器就一直没工作过。

### 3.4 修法与验收

不做任何二进制改动，只要**重新解压且不带 MOTW**（`extract_portable.py` 会清除）。

用程序自带的 B 站播放探针跑验收（免交互、19 秒自动退出）：

```powershell
$env:MABAO_LOCAL_BILI_TEST_URL = 'https://www.bilibili.com/video/BV1hjgG6jEa6/'
Start-Process 'C:\Users\16408\Desktop\二游自动\03_二游辅助\app\二游辅助.exe' `
  -WorkingDirectory 'C:\Users\16408\Desktop\二游自动\03_二游辅助\app'
# 30 秒后看 %LOCALAPPDATA%\MabaoLocal\logs\<今天>.log
```

修复后实测（12:33 那次运行）：

```
[INFO] [mabao.local.bilibili] Bilibili guest script registration queued:
       task=... bytes=52699 sha256=7b4bd663...
[INFO] [mabao.local.bilibili] Bilibili runtime probe:
       MABAO_BILI_PROBE|installed=true|height=1080|ready=4|login=0|
       quality=1080P 高清 1080P 60帧|injected=80|
       ...apiCode=0,hasDash=true,pickedQn=80,lastUrl=.../playurl?...try_look=1&_bohd=1
```

逐项含义：`installed=true` 游客高清脚本注入成功；`apiCode=0` 取流接口成功；`ready=4` 视频已可播放；
`height=1080 / pickedQn=80` 实际拿到 1080P；日志里**没有 CRITICAL**。全程无弹窗、无 UAC。

---

## 4. 顺带发现（影响后续维护，建议决策）

1. **发布版和仓库源码不是同一套代码。** 这份便携版的运行体是「懒人妈宝 6.0 原版模块 + 本地补丁」
   （PyQt5 + pythonnet + WebView2，模块是 `.pyc`，源码只在 `legacy_original\launcher.py` 与 `overrides\` 里），
   而仓库 `src\eryou_assistant\`（PySide6 + qtwebview2）是另一套重写版。两者不能互换。
2. **发布包无法重建。** `legacy_original\build-local.ps1` 要先从原版 `mabao.exe` 里提取 3659 个字节码模块并校验 SHA256，
   而原安装目录 `C:\Program Files (x86)\LanRenMaBao` 已经不在了。所以 `发布存档\` 里那个 zip 是**孤本**，
   建议至少备份一份到别处（网盘/另一块盘），不建议直接删。
3. 以后要修这个工具的功能，只有两条路：改 `launcher.py` 这类补丁层（需要原版模块才能重新打包），
   或者把功能迁到 `src\eryou_assistant\` 那套重写版上（可正常 PyInstaller 构建，有测试）。
