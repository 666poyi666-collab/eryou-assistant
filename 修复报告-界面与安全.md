# 二游辅助 · 界面/登录/安全修复报告（2026-09-20 第二轮）

> 对象：`03_二游辅助\app\`（便携版）与其源码层 `src\legacy_original\`
> 本轮的产物：`二游辅助.exe` sha256 `A6B99FC10ED74959E825BE33BDB0CD7D0090090EDAD35E10EF7860B9826CE56F`
> 旧包留在 `app.previous\`，随时可以整体换回。

---

## 0. 先说一个关键前提：源码改了不等于生效

这个便携版的入口是 **PyInstaller 冻结在 EXE 里的 `launcher.py`**（补丁层），
`src\legacy_original\launcher.py` / `local_features.py` 里的任何改动，**不重新打包就不会生效**。

* 上一次另一个 AI 已经把「提权助手改成开关」「删掉 WS_EX_NOACTIVATE 刷屏」写进了源码，
  但那份改动只在 `src\` 里，用户实际运行的那个 EXE 一点都没变。
* 所以本轮先把**重建管线**打通（`src\legacy_original\build-portable.ps1`），
  再在源码层实现需求，最后重打包、实机验证。

重建之所以可行：原版 `build-local.ps1` 需要 `C:\Program Files (x86)\LanRenMaBao` 的运行时（本机已卸载），
但现有便携包的 `_internal` 就是「原版运行时 + 本地补丁 + 兼容版 numpy/cv2/onnxruntime」，
可以直接当依赖来源 —— 只重编 EXE，几十秒出一个新包。

重建时踩到并修掉的坑（都在脚本里固化了）：
1. PyInstaller 只分析 `launcher.py` 的导入，会把原版 PYZ 里的 stdlib 模块漏掉 121 个；
   实测后果是 `No module named 'fileinput'` → XFeat 模型加载失败 → AI 导航退回 ORB。
   现在 `tools\inspect_pyz.py` 会拿原版 EXE 的 PYZ 清单算出缺口，写进 spec 的 hiddenimports。
2. 新写的 `.ps1` 含中文却没 BOM → PowerShell 5.1 按 ANSI 解析直接语法报错（老坑，又踩了一次）。

---

## 1. 顶部「一键无边框」（新增）

* 标题栏右侧多一个 **⛶** 按钮，点一下进入无边框：收起标题栏与其它界面元素，只留浏览器画面，
  窗口仍可拖动/缩放（用的还是程序自己的 `RESIZE_MARGIN` 缩放逻辑）。
* 无边框状态下屏幕右上角浮出一个 **「还原」** 小窗（`Qt.Tool` + 置顶，能盖在 WebView2 原生窗口上），
  点它或按 **F10** 都能切回来；`F10` 是双向开关。
* 实现文件：`src\legacy_original\borderless.py`（运行时给 `MainWindow` 打补丁，不改原版字节码）。
* 实机验证（运行日志）：

```
[mabao.local.borderless] 无边框模式补丁已装载
[mabao.local.borderless] 无边框按钮已就绪（标题栏 ⛶ / F10）
```

---

## 2. 登录状态与画质

### 2.1 登录态存在哪、会不会丢

内嵌浏览器是 WebView2，登录/Cookies 存在 user data 目录：

```
%LOCALAPPDATA%\懒宝浏览器\WebView2_UserData\EBWebView\Default\Network\Cookies
```

* 这个目录**不在程序目录里**，所以「程序换位置 / 重新解压 / 升级」都不会丢登录；
  之前「一进去就弹登录」的真实原因是 **浏览器从来没起来过**（MOTW 导致 WebView2 初始化失败），
  根本没有机会保存登录态。
* 额外加了一个覆盖开关：设了环境变量 `MABAO_WEBVIEW_PROFILE` 就把 profile 固定到指定目录
  （方便备份/迁移），实现见 `src\legacy_original\overrides\qtwebview2\widget.py`。

### 2.2 画质：原来被一刀切成 1080P

原补丁把每个 `playurl` 请求都改写成 `qn=80`（1080P），**无论有没有登录**，
所以就算你是大会员也拿不到 1080P60/1080P+/4K。现在改成：

* `qn = max(页面请求的画质, 80)`：游客依旧 1080P 试看（`try_look=1` 保留），
  登录后页面请求更高档位时不再被压回去；
* 另注入一段 document-start 脚本（`QUALITY_BOOST_SCRIPT`）：检测到 `DedeUserID`/`SESSDATA` Cookie 时，
  把播放请求的 `qn` 抬到 120（4K），最终档位由服务端按账号权限决定（大会员 → 4K/1080P+；
  普通登录 → 1080P60；未登录 → 1080P 试看）。

实机验证：

```
[mabao.local.bilibili] 画质解锁脚本已注册: bytes=1301
[mabao.local.bilibili] Bilibili runtime probe:
  installed=true|height=1080|ready=4|login=0|quality=1080P 高清 1080P 60帧|injected=80
```

（`login=0` 是探测时没有登录态的游客基线；登录后同一探针会显示更高画质。）

---

## 3. 切窗口/任务栏异常 + 「游戏被杀掉」

### 3.1 任务栏「先最小化又弹回」的根因

原 `launcher.py` 里有个 `sync_game_no_activate()`：只要检测到游戏在前台，
就每 0.25 秒给**进程的所有窗口**刷一次 `WS_EX_NOACTIVATE`（禁止激活）。副作用：

* 任务栏点它 → 系统试图激活 → 窗口拒绝 → 表现成「先缩下去又弹回来」；
* 和 `WA_ShowWithoutActivating`、沉浸/挖孔模式的显示逻辑互相打架。

处理：整段逻辑删除，只保留「设置对话框在游戏前台时被动显示」的温和策略。

### 3.2 「游戏被杀掉」的排查与处置

* **全包审计**：把 `_internal` 里所有 `.pyc/.py/.js` 扫了一遍
  （`taskkill`/`TerminateProcess`/`psutil`/`kill`…）。结论：**程序自身没有任何结束其它进程的代码**；
  `psutil` 只出现在自动对话模块里，用来按进程名**识别**游戏窗口（`Process.name()`），不杀进程。
* 真正的高风险点是**提权输入助手**：老版本启动时把 `mabao-game-input-helper.exe` 复制到
  `C:\Program Files\MabaoLocalInputHelper`，并注册一个 `ONLOGON` + `HIGHEST` 的计划任务，
  它以管理员身份安装**全局键盘/鼠标低级钩子**（`WH_KEYBOARD_LL`/`WH_MOUSE_LL`）并轮询按键。
  这类「提权 + 全局钩子」是反作弊最容易判定为可疑并直接结束游戏的东西。
  处置分两步：
  1. 源码层：助手改成显式开关（只有 `MABAO_ENABLE_ELEVATED_INPUT_HELPER=1` 才启动、才装任务），
     新包里默认完全不起；
  2. 本机遗留清理：已用提权脚本删掉计划任务 `MabaoLocalGameInput`、移除
     `C:\Program Files\MabaoLocalInputHelper`，并结束残留进程（日志：
     `%LOCALAPPDATA%\MabaoLocal\logs\disable-elevated-helper.log`）。
     脚本留在 `tools\disable-elevated-helper.ps1`，换机器可以再跑一次。

> 如果你那次游戏是被**另一个进程**结束的，请对一下时间点：本项目里的
> `一键1-8.ps1` 第 8 步「收尾清理」**按设计**会关掉 BetterGI/游戏。
> 二游辅助本身不会动游戏进程。

---

## 4. 鼠标侧键「只有一个生效」（上一轮遗留问题）

* 你的配置本身没错：`~\.mabao\hotkeys.json` 里 `side_buttons = {x: fast_backward, x2: fast_forward}`。
* 问题是**两个 handler 抢同一批按键**：
  原版 `HotkeyManager` 自带 `WH_MOUSE_LL` 钩子会按同一份配置派发侧键，
  本地 launcher 又用 Raw Input 桥接派发一次；两者叠加（且各自还有 0.14~0.18 秒去抖），
  就表现为「一退一进互相抵消，看起来只有一个键有用」。
* 修法：在原版 `_init_hotkeys` 跑完之后，清掉它自己的侧键回调，**侧键由本地 Raw Input 桥接独占**。
* 实机验证（注入 XBUTTON1/XBUTTON2，读日志）：

```
侧键映射（来自 hotkeys.json side_buttons）: {'x': 'fast_backward', 'x2': 'fast_forward'}
侧键已改由本地 Raw Input 桥接独占（清掉原版低级钩子的 2 个侧键回调）
Raw mouse packet: button_flags=0x40  → Input accepted: action=fast_backward → Native input action: fast_backward
Raw mouse packet: button_flags=0x100 → Input accepted: action=fast_forward  → Native input action: fast_forward
```

---

## 5. 怎么重建、怎么回滚

```powershell
# 改完 src\legacy_original\*.py 之后：重编 EXE + 组装 + 覆盖运行时补丁
powershell -File src\legacy_original\build-portable.ps1

# 组装完直接替换正在用的 app 目录（旧包存成 app.previous）
powershell -File src\legacy_original\build-portable.ps1 -Deploy

# 回滚
Remove-Item -Recurse -Force app; Move-Item app.previous app
```

验收流程（每轮都照这个跑）：

```powershell
# 1) 源码层测试（26 个契约测试）
cd src\legacy_original; ..\..\build\venv\Scripts\python.exe -m pytest tests -q

# 2) 实机：跑自带 B 站探针，看日志没有 CRITICAL、能拿到 1080P
$env:MABAO_LOCAL_BILI_TEST_URL = 'https://www.bilibili.com/video/BV1hjgG6jEa6/'
Start-Process .\app\二游辅助.exe -WorkingDirectory .\app
# 30 秒后看 %LOCALAPPDATA%\MabaoLocal\logs\<今天>.log
```

---

## 6. 还没做 / 需要你确认的

* **画质上限依赖登录**：4K/1080P+ 需要大会员账号，程序只能「不再人为压到 1080P」。
  如果你登录后探针里 `realQ` 还是 80，把当次日志发我，我看服务端返回了什么。
* **无边框模式目前不记忆**：重启程序回到有边框状态（有意为之，避免「一开机就没标题栏」找不到按钮）。
  想要记忆的话，下一篇可以写进 `window.json`。
* **发布包**：`v0.2.1` 的 Release 里会放这份修好的便携包；
  老 `v0.2.0` 包别再用（中文名乱码 + MOTW 会让浏览器起不来）。
